<!-- -*- Mode: html -*- -->
<header class="post-header">
  <h1 class="post-title">{{title}}</h1>
  <section class="post-meta">
    <time class="post-date" datetime="{{date}}">{{formattedDate}}</time>
    <!-- For each tag, create a link to the page showing the articles with it -->
    {{#if tags}}
    &#x2615;
    {{#each tags}}
    {{#if @index}} • {{/if}}
    <a href="/topics/{{urlSafe}}.html">{{tag}} ({{articleCount}})</a>
    {{/each}}
    {{/if}}
  </section>
</header>
