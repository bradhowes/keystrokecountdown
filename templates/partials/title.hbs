<!-- -*- Mode: html -*- -->
<header class="post-header">
  <h1 class="post-title">{{title}}</h1>
  <section class="post-meta">
    <time class="post-date" datetime="{{date}}">{{formattedDate}}</time>{{#if tags}} &#x2615;
    {{#each tags}}
    {{#if @index}} • {{/if}}<a class="post-date" href="/topics/{{urlSafe}}.html">{{tag}} ({{articleCount}})</a>
    {{/each}}
    {{/if}}
  </section>
</header>
