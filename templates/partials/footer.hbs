<!-- -*- Mode: html -*- -->
<footer class="site-footer clearfix">

  <!-- Show a small footer asserting our copyright. The year here will be taken from the `date` from the
       document header. However, when the page renders in a browser Javascript - if enabled - will overwrite
       it with the current year. That way we keep the year up-to-date (not really necessary), without having
       source changes in all of the articles when the year changes.
    -->
  <section class="copyright"><a href="{{site.url}}">{{site.title}}</a> &copy;
    <span id="copyrightYear">{{date format="YYYY"}}</span> Brad Howes —
    <a href="https://github.com/bradhowes/keystrokecountdown">Source</a></section>
</footer>
